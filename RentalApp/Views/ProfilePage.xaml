<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RentalApp.Views.ProfilePage"
             Title="Profile">
    <ScrollView>
        <VerticalStackLayout Padding="22" Spacing="14" MaximumWidthRequest="620">
            <VerticalStackLayout>
                <Label Text="Your profile" Style="{StaticResource PageTitle}" />
                <Label Text="Account details and marketplace reputation."
                       Style="{StaticResource MutedLabel}" />
            </VerticalStackLayout>

            <Border BackgroundColor="{StaticResource PrimaryDark}" StrokeThickness="0"
                    StrokeShape="RoundRectangle 18" Padding="22">
                <VerticalStackLayout Spacing="7">
                    <Label Text="{Binding Profile.DisplayName}" FontSize="26" FontAttributes="Bold" TextColor="White" />
                    <Label Text="{Binding Profile.Email}" TextColor="#DBEAFE" />
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,12,0,0">
                        <Border BackgroundColor="#334E9A" StrokeThickness="0" StrokeShape="RoundRectangle 12" Padding="12">
                            <VerticalStackLayout>
                                <Label TextColor="White" FontSize="20" FontAttributes="Bold">
                                    <Label.Text>
                                        <MultiBinding StringFormat="★ {0:F1} ({1})">
                                            <Binding Path="Profile.AverageRating" />
                                            <Binding Path="Profile.ReviewCount" />
                                        </MultiBinding>
                                    </Label.Text>
                                </Label>
                                <Label Text="Community rating" TextColor="#DBEAFE" FontSize="12" />
                            </VerticalStackLayout>
                        </Border>
                        <Border Grid.Column="1" BackgroundColor="#334E9A" StrokeThickness="0" StrokeShape="RoundRectangle 12" Padding="12">
                            <VerticalStackLayout>
                                <Label Text="List + rent" TextColor="White"
                                       FontSize="20" FontAttributes="Bold" />
                                <Label Text="Full account access" TextColor="#DBEAFE" FontSize="12" />
                            </VerticalStackLayout>
                        </Border>
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <Label Text="{Binding ErrorMessage}" Style="{StaticResource ErrorLabel}" />
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"
                               Color="{StaticResource Primary}" />
            <Button Text="Sign out" Command="{Binding LogoutCommand}" Style="{StaticResource DangerButton}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
